---
- name: Unarchive artifact.tar.gz to EC2
  become: true
  unarchive:
    src: /workspace/project
    dest: ~/
    remote_src: True

- name: npm install and start
  become: true
  shell: |
    cd ~/
    ls
    cd ~/artifact
    npm install
    pm2 stop default
    pm2 start npm -- start