---
- name: Unarchive artifact.tar.gz to EC2
    src: /workspace/project
    dest: /workspace/project
    remote_src: True

- name: Print env variable
    shell: |
      cd /workspace/project/backend
      npm install
      pm2 stop default
      pm2 start npm -- start

      exit 0